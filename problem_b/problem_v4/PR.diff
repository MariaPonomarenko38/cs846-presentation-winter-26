diff --git a/backend/user_helpers.py b/backend/user_helpers.py
new file mode 100644
index 0000000..abc1234
--- /dev/null
+++ b/backend/user_helpers.py
@@
+"""
+Backend helpers for user display and lookup, used by the dashboard service.
+"""
+
+import requests
+
+
+def get_user_display(user_id):
+    r = requests.get("https://api.example.com/users/" + str(user_id))
+    data = r.json()
+    return data["name"], data["email"]
+
+
+def get_user_list(role=None):
+    url = "https://api.example.com/users"
+    if role:
+        url += "?role=" + role
+    r = requests.get(url)
+    users = r.json()
+    result = []
+    for u in users:
+        result.append(
+            {
+                "id": u["id"],
+                "name": u["name"],
+                "email": u["email"],
+                "role": u.get("role", "user"),
+            }
+        )
+    return result
+
+
+def format_user_for_header(user_id):
+    name, email = get_user_display(user_id)
+    user_list = get_user_list()
+    for u in user_list:
+        if u["id"] == user_id:
+            return f"{name} ({u['role']})"
+    return name
+
+
+def lookup_by_email(email):
+    users = get_user_list()
+    for u in users:
+        if u["email"] == email:
+            return u["id"]
+    return None