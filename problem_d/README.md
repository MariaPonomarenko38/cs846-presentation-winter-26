# Problem D

Control panel for seeding the database and launching services.

## Setup

- Install Python dependency: `pip install console-menu`
- (Optional) install Node dependencies in backend and frontend.

## Usage

```bash
./manage.py
```

## Notes

- Backend uses `problem_d_database/northwind_signal.sqlite` by default.
- Frontend expects backend on `http://localhost:4000`.
